before_install:
  - gem install bundler
  - gem install appraisal
  - gem install rspec-rails
rvm:
  - 1.9.3
  - jruby-19mode
  - 2.0.0 
env:
  - RAILS_ENV=test RACK_ENV=test
branches:
  only:
    - master
    - develop  
